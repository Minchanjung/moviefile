extends layout 

block content 
    h1= title 

    ul 
        each movie in movie_list 
            li 
                a(href=movie.url) 
                    img(src=`${movie.img}`)
                    div #{movie.title} by #{movie.director.name}
        
        else 
            li There are no movies.