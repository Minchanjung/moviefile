extends layout

block content

  h1 Genre: #{data[0].name}

  div(style='margin-left:20px;margin-top:20px')

    h4 Movies

    dl
      each movie in data[1]
        dt
          a(href=movie.url) #{movie.title}
        dd #{movie.summary}

      else
        p This genre has no movies